{% extends 'admin/index.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1 class="h1-Admin">Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ article.date ? article.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_index') }}"><button class="btn btn-secondary">Retour à la liste</button></a>

    <a href="{{ path('article_edit', {'id': article.id}) }}"><button class="btn btn-secondary">Modifier</button></a>

    {{ include('admin/article/_delete_form.html.twig') }}
{% endblock %}
