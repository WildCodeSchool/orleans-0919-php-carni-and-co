{% extends 'admin/index.html.twig' %}

{% block title %}Editer un article{% endblock %}

{% block body %}
    <div class="flex-column">
        <h1 class="h1-Admin">Editer un article</h1>

        <div class="flex-column">

            {{ include('admin/article/_form.html.twig', {'button_label': 'Modifier'}) }}
            {{ include('admin/article/_delete_form.html.twig') }}
        </div>
        <a href="{{ path('article_index') }}">
            <button class="btn btn-secondary mb-1">Retour à la liste</button>
        </a>

    </div>
{% endblock %}
