{% extends 'admin/index.html.twig' %}

{% block title %}Nutrient{% endblock %}

{% block body %}
    <h1 class="h1-Admin">Nutrient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nutrient.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nutrient.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ nutrient.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ nutrient.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nutrient_index') }}">back to list</a>

    <a href="{{ path('nutrient_edit', {'id': nutrient.id}) }}">edit</a>

    {{ include('/admin/nutrient/_delete_form.html.twig') }}
{% endblock %}
