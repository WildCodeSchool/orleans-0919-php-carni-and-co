<form method="post" action="{{ path('faq_delete', {'id': faq.id}) }}" onsubmit="return confirm('Etes vous sûr de supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faq.id) }}">
    <button class="btn btn-danger button-suppr-nutrient">Supprimer la question</button>
</form>
