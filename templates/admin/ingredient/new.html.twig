{% extends 'admin/index.html.twig' %}

{% block title %}Nouvel Ingredient{% endblock %}

{% block body %}
    <div class="flex-column">
        <h1 class="h1-Admin">Ajouter un nouvel ingrédient</h1>

        <div class="flex-column">
            {{ include('/admin/ingredient/_form.html.twig') }}
        </div>
        <a href="{{ path('ingredient_index') }}"><button class="btn btn-secondary mb-1">Retour à la liste</button></a>
    </div>
{% endblock %}
