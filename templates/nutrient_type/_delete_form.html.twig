<form method="post" action="{{ path('nutrient_type_delete', {'id': nutrient_type.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce type de nutriment?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutrient_type.id) }}">
    <button class="btn btn-danger mt-3">Supprimer</button>
</form>
