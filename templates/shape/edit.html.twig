{% extends 'admin/index.html.twig' %}

{% block title %}Edit Shape{% endblock %}

{% block body %}
    <h1>Edit Shape</h1>

    {{ include('shape/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shape_index') }}">back to list</a>

    {{ include('shape/_delete_form.html.twig') }}
{% endblock %}
