{% extends 'admin/index.html.twig' %}

{% block title %}Shape{% endblock %}

{% block body %}
    <h1>Shape</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shape.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shape.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shape_index') }}">back to list</a>

    <a href="{{ path('shape_edit', {'id': shape.id}) }}">edit</a>

    {{ include('shape/_delete_form.html.twig') }}
{% endblock %}
