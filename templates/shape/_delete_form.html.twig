<form method="post" action="{{ path('shape_delete', {'id': shape.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette forme?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shape.id) }}">
    <button class="btn btn-danger mt-3">Supprimer</button>
</form>
