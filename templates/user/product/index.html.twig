{% extends 'layout.html.twig' %}

{% block title %}Liste des produits{% endblock %}

{% block body %}
    <div class="header-user-product">
        <h1 class="h1-User">Tous les produits</h1>
    </div>
    <div class="row">
        <div class="col-3">
            {{ form_start(formfilter,{'method': 'GET'}) }}
            {{ form_widget(formfilter) }}
            <button class="btn btn-secondary w-100">{{ button_label|default('Rechercher') }}</button>
            {{ form_end(formfilter) }}
        </div>
        <div class="col-9">
            <div class="container-user-product">
                <div class="all-card-user-product">
                    {% for product in products %}
                        <div class="card card-product-user bg-light mb-3">
                            <div class="card-header card-header-user">{{ product.reference }}</div>
                            <div class="card-body">
                                <p>{{ product.animal.name }}</p>
                                <p>Marque : {{ product.brand.name }}</p>
                                <p>Type : {{ product.food.type }}</p>
                                <a class="btn btn-secondary" href="{{ path('product_show', {'id': product.id}) }}"
                                   role="button">Plus d'infos</a>
                            </div>
                        </div>
                    {% else %}
                        <p>Pas de produits trouvés</p>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
    {{ knp_pagination_render(products) }}
{% endblock %}
