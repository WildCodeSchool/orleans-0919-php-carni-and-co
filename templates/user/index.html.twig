{% extends 'layout_admin.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <h1 class="h1-Admin">Liste des utilisateurs</h1>

    <div class="col-3">
        {{ form_start(form,{'method': 'GET'}) }}
        {{ form_widget(form) }}
        <button class="btn btn-secondary w-100">{{ button_label|default('Rechercher') }}</button>
        {{ form_end(form) }}
    </div>

    <table class="table-admin">
        <thead class="header-table-admin">
            <tr>
                <th>Username</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody class="body-table-admin">
        {% for user in users %}
            <tr>
                <td>{{ user.username }}</td>
                <td>{{ user.email }}</td>
                <td>
                    <a href="{{ path('user_show', {'id': user.id}) }}"><button class="btn btn-secondary mb-4">Voir</button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun utilisateur trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {{ knp_pagination_render(users) }}
{% endblock %}
