{% extends 'admin/index.html.twig' %}

{% block title %}New Origin{% endblock %}

{% block body %}
    <div class="flex-column">
        <h1 class="mt-3">Ajouter une nouvelle origine d'ingrédient</h1>

        <div class="flex-column">
            {{ include('origin/_form.html.twig') }}
        </div>
        <a href="{{ path('origin_index') }}"><button class="btn btn-secondary mb-1">Retour à la liste</button></a>
    </div>
{% endblock %}
