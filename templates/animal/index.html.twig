{% extends 'admin/index.html.twig' %}

{% block title %}Animal index{% endblock %}

{% block body %}
    <h1 class="animalTitle">Liste des animaux</h1>

    <a class="title_create" href="{{ path('animal_new') }}">Ajouter</a>

        {% for animal in animals %}
            <div class="card mb-3 margeHome">
                <div class="row no-gutters">
                    <div class="col-md-4 image">
                        <img src="{{ asset( '/build/'~animal.image ) }}" class="card-img"
                             alt="Photo {{ animal.name }}">
                    </div>
                    <div class="col-md-8 text">
                        <div class="card-body textcard">
                            <h2 class="card-title">{{ animal.name | title }}</h2>
                            <p>
                                <a href="{{ path('animal_show', {'id': animal.id}) }}">Voir</a>
                                <a href="{{ path('animal_edit', {'id': animal.id}) }}">Editer</a>
                            </p>
                            <p class="card-text">
                                {{ animal.description }}
                            </p>
                        </div>
                    </div>
                </div>
            </div>

        {% else %}
                <p>Aucun animal trouvé.</p>
        {% endfor %}

{% endblock %}
