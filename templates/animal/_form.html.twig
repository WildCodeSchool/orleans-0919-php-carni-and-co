{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.image) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
