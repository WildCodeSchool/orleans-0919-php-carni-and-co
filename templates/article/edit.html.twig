{% extends 'base.html.twig' %}

{% block title %}Editer un article{% endblock %}

{% block body %}
    <div class="flex-column">
        <h1 class="mt-3">Editer un article</h1>

        <div class="flex-column">

            {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

            {{ include('article/_delete_form.html.twig') }}
        </div>
        <a href="{{ path('article_index') }}">
            <button class="btn btn-secondary mb-1">Retour à la liste</button>
        </a>

    </div>
{% endblock %}
