{% extends 'base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <div class="flex-column text-center">
        <h1 class="h1_admin_blog">Article index</h1>

        <a href="{{ path('article_new') }}">
            <button class="btn btn-secondary mb-3 center-button">Enregistrer un article</button>
        </a>
        <table class="table">
            <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Date</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for article in articles %}
                <tr>
                    <td>{{ article.id }}</td>
                    <td>{{ article.title }}</td>
                    <td>{{ article.description }}</td>
                    <td>{{ article.date ? article.date|date('Y-m-d H:i:s') : '' }}</td>
                    <td>{{ article.image }}</td>
                    <td>
                        <a href="{{ path('article_show', {'id': article.id}) }}">show</a>

                        <a href="{{ path('article_edit', {'id': article.id}) }}">
                            <button class="btn btn-secondary">Modifier</button>
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">Pas d'article enregistré</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>


    </div>
{% endblock %}
