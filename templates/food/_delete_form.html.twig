<form method="post" action="{{ path('food_delete', {'id': food.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet aliment');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ food.id) }}">
    <button class="btn btn-danger mt-3">Supprimer</button>
</form>
