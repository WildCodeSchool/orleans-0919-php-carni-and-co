{% extends 'admin/index.html.twig' %}

{% block title %}New Food{% endblock %}

{% block body %}
<div class="flex-column">
    <h1 class="mt-3">Ajouter un nouvel aliment</h1>

    <div class="flex-column">
    {{ include('food/_form.html.twig') }}
    </div>

    <a href="{{ path('food_index') }}"><button class="btn btn-secondary mb-1">Retour à la liste</button></a>
</div>
{% endblock %}
