<form method="post" action="{{ path('bring_delete', {'id': bring.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette apport??');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bring.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
